#include "tree.h"

typedef struct tree_node {

} tree_node;

tree_t tree_create_node(any_t data)
{
    
}

void tree_destroy_node(tree_t);
void tree_append_node(tree_t, tree_t);
void tree_dump_node(tree_t);